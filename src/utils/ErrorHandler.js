// ErrorHandler Implementation

class ErrorHandler {
    static handleError(error) {
        console.error('Error:', error);
        // Simple error logging or additional handling can be added here
    }
}

module.exports = ErrorHandler;